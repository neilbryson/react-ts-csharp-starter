import React, { ReactElement } from 'react';

export const App = (): ReactElement => {
  return (
    <div className="w-screen h-screen bg-blue-100 text-gray-800">
      <h1 className="font-bold text-4xl">It works!</h1>
    </div>
  );
};
